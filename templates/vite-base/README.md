# Vite + React base template

Vite + React + TypeScript + TanStack Router + Tailwind v4 + shadcn/ui (Base UI primitives) + zod.

## After scaffolding

```bash
pnpm install
npx shadcn@latest add button   # add components as needed
pnpm dev
```
