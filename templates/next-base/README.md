# Next.js base template

Next.js App Router + TypeScript + Tailwind v4 + shadcn/ui (Base UI primitives) + zod.

## After scaffolding

```bash
pnpm install
npx shadcn@latest add button   # add components as needed
pnpm dev
```
